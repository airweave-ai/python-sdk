# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ...types.agentic_search_event import AgenticSearchEvent
from ...types.filter_group import FilterGroup
from ...types.retrieval_strategy import RetrievalStrategy
from ...types.search_v_2_response import SearchV2Response
from .raw_client import AsyncRawSearchClient, RawSearchClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SearchClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSearchClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSearchClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSearchClient
        """
        return self._raw_client

    def instant(
        self,
        readable_id: str,
        *,
        query: str,
        retrieval_strategy: typing.Optional[RetrievalStrategy] = OMIT,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        offset: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchV2Response:
        """
        Direct vector search.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        retrieval_strategy : typing.Optional[RetrievalStrategy]
            Which retrieval strategy to use.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results to return.

        offset : typing.Optional[int]
            Number of results to skip.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchV2Response
            Successful Response

        Examples
        --------
        from airweave import AirweaveSDK

        client = AirweaveSDK(
            api_key="YOUR_API_KEY",
        )
        client.collections.search.instant(
            readable_id="customer-support-tickets-x7k9m",
            query="How do I reset my password?",
            retrieval_strategy="hybrid",
            limit=10,
        )
        """
        _response = self._raw_client.instant(
            readable_id,
            query=query,
            retrieval_strategy=retrieval_strategy,
            filter=filter,
            limit=limit,
            offset=offset,
            request_options=request_options,
        )
        return _response.data

    def classic(
        self,
        readable_id: str,
        *,
        query: str,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        offset: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchV2Response:
        """
        AI-optimized search.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results to return.

        offset : typing.Optional[int]
            Number of results to skip.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchV2Response
            Successful Response

        Examples
        --------
        from airweave import AirweaveSDK

        client = AirweaveSDK(
            api_key="YOUR_API_KEY",
        )
        client.collections.search.classic(
            readable_id="customer-support-tickets-x7k9m",
            query="quarterly revenue report",
            limit=10,
        )
        """
        _response = self._raw_client.classic(
            readable_id, query=query, filter=filter, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    def agentic(
        self,
        readable_id: str,
        *,
        query: str,
        thinking: typing.Optional[bool] = OMIT,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchV2Response:
        """
        Agent that iteratively searches, reads, navigates hierarchies, and collects results.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        thinking : typing.Optional[bool]
            Enable extended thinking / chain-of-thought.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results. None means agent decides.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchV2Response
            Successful Response

        Examples
        --------
        from airweave import AirweaveSDK

        client = AirweaveSDK(
            api_key="YOUR_API_KEY",
        )
        client.collections.search.agentic(
            readable_id="customer-support-tickets-x7k9m",
            query="find all deployment-related docs from last month",
            thinking=True,
        )
        """
        _response = self._raw_client.agentic(
            readable_id, query=query, thinking=thinking, filter=filter, limit=limit, request_options=request_options
        )
        return _response.data

    def stream_agentic(
        self,
        readable_id: str,
        *,
        query: str,
        thinking: typing.Optional[bool] = OMIT,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[AgenticSearchEvent]:
        """
        Streaming agentic search via Server-Sent Events. Returns real-time events as the agent searches.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        thinking : typing.Optional[bool]
            Enable extended thinking / chain-of-thought.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results. None means agent decides.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Yields
        ------
        typing.Iterator[AgenticSearchEvent]
            SSE stream of agentic search events.

        Examples
        --------
        from airweave import AirweaveSDK

        client = AirweaveSDK(
            api_key="YOUR_API_KEY",
        )
        response = client.collections.search.stream_agentic(
            readable_id="readable_id",
            query="query",
        )
        for chunk in response:
            yield chunk
        """
        with self._raw_client.stream_agentic(
            readable_id, query=query, thinking=thinking, filter=filter, limit=limit, request_options=request_options
        ) as r:
            yield from r.data


class AsyncSearchClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSearchClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSearchClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSearchClient
        """
        return self._raw_client

    async def instant(
        self,
        readable_id: str,
        *,
        query: str,
        retrieval_strategy: typing.Optional[RetrievalStrategy] = OMIT,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        offset: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchV2Response:
        """
        Direct vector search.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        retrieval_strategy : typing.Optional[RetrievalStrategy]
            Which retrieval strategy to use.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results to return.

        offset : typing.Optional[int]
            Number of results to skip.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchV2Response
            Successful Response

        Examples
        --------
        import asyncio

        from airweave import AsyncAirweaveSDK

        client = AsyncAirweaveSDK(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.collections.search.instant(
                readable_id="customer-support-tickets-x7k9m",
                query="How do I reset my password?",
                retrieval_strategy="hybrid",
                limit=10,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.instant(
            readable_id,
            query=query,
            retrieval_strategy=retrieval_strategy,
            filter=filter,
            limit=limit,
            offset=offset,
            request_options=request_options,
        )
        return _response.data

    async def classic(
        self,
        readable_id: str,
        *,
        query: str,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        offset: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchV2Response:
        """
        AI-optimized search.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results to return.

        offset : typing.Optional[int]
            Number of results to skip.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchV2Response
            Successful Response

        Examples
        --------
        import asyncio

        from airweave import AsyncAirweaveSDK

        client = AsyncAirweaveSDK(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.collections.search.classic(
                readable_id="customer-support-tickets-x7k9m",
                query="quarterly revenue report",
                limit=10,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.classic(
            readable_id, query=query, filter=filter, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    async def agentic(
        self,
        readable_id: str,
        *,
        query: str,
        thinking: typing.Optional[bool] = OMIT,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SearchV2Response:
        """
        Agent that iteratively searches, reads, navigates hierarchies, and collects results.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        thinking : typing.Optional[bool]
            Enable extended thinking / chain-of-thought.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results. None means agent decides.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SearchV2Response
            Successful Response

        Examples
        --------
        import asyncio

        from airweave import AsyncAirweaveSDK

        client = AsyncAirweaveSDK(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.collections.search.agentic(
                readable_id="customer-support-tickets-x7k9m",
                query="find all deployment-related docs from last month",
                thinking=True,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.agentic(
            readable_id, query=query, thinking=thinking, filter=filter, limit=limit, request_options=request_options
        )
        return _response.data

    async def stream_agentic(
        self,
        readable_id: str,
        *,
        query: str,
        thinking: typing.Optional[bool] = OMIT,
        filter: typing.Optional[typing.Sequence[FilterGroup]] = OMIT,
        limit: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[AgenticSearchEvent]:
        """
        Streaming agentic search via Server-Sent Events. Returns real-time events as the agent searches.

        Parameters
        ----------
        readable_id : str

        query : str
            Search query text.

        thinking : typing.Optional[bool]
            Enable extended thinking / chain-of-thought.

        filter : typing.Optional[typing.Sequence[FilterGroup]]
            Filter groups (combined with OR).

        limit : typing.Optional[int]
            Max results. None means agent decides.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Yields
        ------
        typing.AsyncIterator[AgenticSearchEvent]
            SSE stream of agentic search events.

        Examples
        --------
        import asyncio

        from airweave import AsyncAirweaveSDK

        client = AsyncAirweaveSDK(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.collections.search.stream_agentic(
                readable_id="readable_id",
                query="query",
            )
            async for chunk in response:
                yield chunk


        asyncio.run(main())
        """
        async with self._raw_client.stream_agentic(
            readable_id, query=query, thinking=thinking, filter=filter, limit=limit, request_options=request_options
        ) as r:
            async for _chunk in r.data:
                yield _chunk
